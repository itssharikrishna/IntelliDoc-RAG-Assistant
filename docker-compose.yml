services:
  rag:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./web:/app/web
    environment:
      - PYTHONUNBUFFERED=1
      - SECRET_KEY=SUPER_SECRET_KEY_CHANGE_THIS
    restart: unless-stopped
